没问题，这是将项目简介、Docker 部署配置、Bot 交互指南以及 Web UI 图文操作完整整合后的 `README.md` 最终版本。你可以直接整体复制并提交到你的项目仓库中。

---

# PS2115 - 影视资源自动化聚合与转存系统 🎬

PS2115 是一个专为 115 网盘用户打造的影视资源自动化管理工具。它集成了强大的 Telegram Bot 交互、多源资源聚合搜索、全自动订阅追剧以及 115 网盘的深度本地化管理（包含增量同步与事件监控）。

## ✨ 核心特性

* **🤖 极致的 Telegram Bot 交互**：采用高屏效比设计，防误触 URL 按钮排版，实现极简的搜索、转存、订阅体验。
* **🔍 双引擎资源聚合**：
* `CloudSaver`：直接抓取各大 Telegram 公开资源频道，深度接入自动订阅系统。
* `Pansou`：独立聚合搜索引擎 API，提供全网泛搜（仅用于直接转存/离线，无缝补充资源库）。


* **⚡ 115 深度整合与防风控**：
* 内置 115 生活事件增量监控，支持特定 CID 的精准捕获与目录树血缘校验。
* 适配 115 最新 Pro API 加密与解密逻辑（解决 `decode fail!` 报错）。
* 严格且安全的 QPS 控制（动态节流至 0.5 - 1.0），保护账号免受风控。


* **🚀 高性能架构**：后台 Cron 定时拉取与前端 API 手动触发双引擎策略；针对播放器设计的 UA 前缀智能缓存机制（大幅减少 302 重定向请求频率）。

---

## 📦 快速部署 (Docker)

推荐使用 Docker Compose 进行一键部署。

### 1. 编写 `docker-compose.yml`

```yaml
version: '3.8'

services:
  ps2115:
    image: lisylva/ps2115:latest # 或指定固定版本号
    container_name: ps2115
    restart: unless-stopped
    ports:
      - "8080:8080" # Web UI / API 端口
      - "8099:8099" # 内部通信/中间件反代端口
    volumes:
      - ./data:/app/data
      - ./strm:/app/strm
      - ./config.yaml:/app/config.yaml
    environment:
      - TZ=Asia/Shanghai

```

### 2. 准备配置文件

在映射的目录下创建 `config.yaml`，参考以下配置：

```yaml
# 基础服务配置
server:
  username: admin
  password: admin123
  jwt_secret: ps2115-secret-key-change-me

# CloudSaver （接入订阅系统）
cloudsaver:
  channels:
    - id: "xxxx"
      alias: "影视频道名称"


# Pansou 聚合搜索 API：独立搜索引擎（用于临时搜索及转存，不接入订阅）
pansou:
  url: "https://so.252035.xyz/"

```

### 3. 启动服务

```bash
docker-compose up -d

```

---

## 📱 Telegram Bot 交互指南

绑定并启动 Bot 后，你可以通过以下命令在 Telegram 中直接管理你的媒体库：

| 命令 | 参数 | 说明 |
| --- | --- | --- |
| `/search` | `<关键词>` | 搜索 115 资源（走 Cloudsaver 引擎，支持订阅联动） |
| `/ps` | `<关键词>` | 盘搜搜索（走 Pansou 引擎，支持直接转存/离线下载） |
| `/sub add` | `<关键词> | <目录>` | 添加新的追剧订阅规则 |
| `/sub list` | 无 | 查看当前所有订阅任务的状态 |
| `/sub del` | `<ID>` | 删除指定的订阅 |
| `/sub pause` | `<ID>` | 暂停指定的订阅任务 |
| `/sub resume` | `<ID>` | 恢复已暂停的订阅任务 |
| `/sub check` | `<ID>` | 立即触发一次订阅检查，拉取最新资源 |
| `/status` | 无 | 查看系统运行状态、资源占用及 QPS 监控 |
| `/cookie` | `<cookie>` | 快速更新或重置 115 网盘的 Cookie 凭证 |
| `/help` | 无 | 显示使用帮助与命令列表 |

---

## ⚙️ Web UI 操作指南

通过 Web 界面，你可以直观地管理分享链接、个人网盘目录，并生成 Emby/Jellyfin 兼容的 STRM 播放文件。

### 一、 分享链接批量生成 STRM (分享源入库)

*适用场景：将别人分享的 115 链接直接转化为本地媒体库的 STRM 文件。*

1. **参数配置**：
* **STRM 输出目录**：填写容器内的映射路径。例如 Docker 映射为 `./strm:/strm`，建议填写 `/strm/分享` 进行分类。
* **服务地址**：填写供播放器反代访问中间件的地址，通常为 `http://<你的IP>:8099`（默认 8099 端口）。


2. **扫描入库**：在【分享索引管理】中贴入带密码的 115 分享链接，点击 `[扫描入库]` 将结构抓取至本地 DB 缓存。
3. **生成文件**：在入库列表中，点击单条记录右侧的 `[生成]` 按钮，或使用全局的 `[生成 STRM 文件并写入磁盘]` 进行批量生成。

### 二、 网盘目录批量生成与增量同步 (个人盘入库)

*适用场景：将自己 115 盘内的目录同步为 STRM 文件，并保持自动化增量更新。*

1. **选取 CID**：在【选择 115 网盘目录】区块，点击右上角刷新，从目录树中精准选取要入库的文件夹 CID（如：电影库）。
2. **防风控扫盘**：选定目录后，点击 `[扫描入库(防风控)]`，系统将以平滑速率将该目录文件存入 DB 缓存。
3. **导入 STRM**：缓存建立后，点击 `[全量索引导入 STRM]` 写入本地磁盘。
4. **自动化同步与防风控策略 (核心)**：
* **定时增量**：启用定时增量开关，**确保填写的“定时目录 CID”与扫描入库的 CID 完全一致**（可点击 `[使用当前选择]` 快速填入）。
* **间隔巡检 L2**：实时事件监控的**兜底设置**，建议启用。定期比对目录 Hash，捕捉漏掉的变动。
* **全树 Sweep L3**：**⚠️ 强烈警告：如果 115 网盘总文件库庞大，极度不建议开启**。全树扫描极易触发 115 风控限制，日常使用 L1（实时事件）和 L2 已能保证高度一致性。

---

## 🛠️ 架构与高阶功能说明

### 115 网盘事件监控与独立控制

本项目并非单纯的定时全量扫盘。系统实现了**三层独立控制机制**：

1. **Layer 1 (实时)**：生活事件增量同步（精准抓取变化）。
2. **Layer 2 (兜底)**：间隔巡检 CID 列表。
3. **Layer 3 (重构)**：全树巡检 CID 配置（仅在极端情况下开启）。
数据持久化采用 SQLite 的 JSON 序列化存储，确保防风控限速下万级文件的平滑扫盘，即便中断也能自我修复对齐。

### 搜索引擎差异说明

* **搜索资源 (Cloudsaver)**：结果会作用于订阅管理系统。当配置的 TG 频道发布新资源时，系统会自动匹配您的 `/sub` 规则完成自动化转存。
* **盘搜 (Pansou)**：作为资源补充的独立入口，查到的资源仅供即时 `[📥 转存]` 或 `[⬇ 离线]` 使用，**不会**污染或触发你的日常自动化订阅流。

---

* Draft a CHANGELOG for recent versions
* Create GitHub issue templates
* Format release notes for GitHub
